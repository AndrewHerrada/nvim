{
  "competitive programming setup": {
    "prefix": "cpp",
    "body": [
      "#include <bits/stdc++.h>",
      "using namespace std;",
      "",
      "typedef long long ll;",
      "typedef pair<int,int> ii;",
      "typedef vector<int> vi;",
      "typedef vector<ii> vii;",
      "//memset(memo, -1, sizeof memo);            // inicializar tabla de memorizacion de DP",
      "//vi memo(n, -1);                           // método alternativo",
      "//memset(arr, 0, sizeof arr);               // vaciado de un array de enteros",
      "const int INF = 1e9;                      // 10^9 = 1B es < 2^31 - 1",
      "const int LLINF = 4e18;                   // 4*10^18 es < 2^63 - 1",
      "const double EPS = 1e-9;                  // un numero muy pequeño",
      "",
      "int main() {",
      "\tios_base::sync_with_stdio(false);",
      "\tcin.tie(NULL);",
      "\t",
      "\t$0",
      "\t",
      "\treturn 0;",
      "}"
    ],
    "desc": "All competitive programming setup code"
  }
}
